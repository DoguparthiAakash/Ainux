TODO list
=========

1. (URGENT) Fix the broken manpages that doxy2man produces ("\n" gets turned
   into '0').
   Other manpage weirdness: might be better to simply produce a program
   to produce manpages

2. Figure out what would be nice additions to xstring:
   a) string-type (determine type, if any, represented by a string?)

4. Rudimentary configuration management:
   a) Use config variables to store errors that occur (xerror)

5. (URGENT) Fix all the warnings issued by xdict
   **Ignored - all warnings issued by test harness**

6. Some sort of design for reading in xcobs from files

7. Add ability to retrieve child objects by name - xcob

8. (URGENT) Toplevel makefile must pass in its own name for recursive builds.
   This lets the lower-level makefiles use the correct -f argument to make.
