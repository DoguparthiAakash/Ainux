[package]
name = "memory_rs"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
linked_list_allocator = "0.10.5"
spin = "0.9.8"

[profile.release]
panic = "abort"
opt-level = "s"
lto = true
