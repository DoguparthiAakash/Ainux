# Nux Standalone Build Instructions

> [!WARNING]
> The `nux_portable` standalone version is currently missing from this repository. 
> These instructions are disabled until the standalone version is added back.

The Ainux kernel's internal Nux VM and Compiler are also currently disabled in the kernel source to ensure build stability while the standalone version is being refactored.

We will add it later.
